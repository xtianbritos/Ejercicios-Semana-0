const potencia = (base: number, exponente: number): void => {
    console.log(`El número ${base} elevado al exponente ${exponente} da ${Math.pow(base, exponente)}`);
}

potencia(5, 3);
potencia(20, 2);
potencia(1, 20);